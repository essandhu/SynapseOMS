global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "gateway"
    static_configs:
      - targets: ["gateway:8080"]

  - job_name: "risk-engine"
    static_configs:
      - targets: ["risk-engine:8081"]

  # Kafka consumer lag metrics — add when JMX exporter is configured:
  # - job_name: "kafka"
  #   static_configs:
  #     - targets: ["kafka:9404"]
